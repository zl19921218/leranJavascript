function oneLevel(str) { 
    return /\*\//.test(str);
}

function twolevel(str) { 
    return /\+\-/.test(str);
}

function addBarackets(str) { 
    let i = 0, isFirst = true;
    while (i < str.length) { 
        // if ()
    }
}